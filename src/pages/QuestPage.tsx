import { StatusPanel } from "../components/StatusPanel";

export function QuestPage() {
  return (
    <StatusPanel title="Active quest" eyebrow="Narrative placeholder">
      Short AI-generated scenes, 2-3 choices, and one-turn choice spending will be
      implemented after story turns and quest state are in place.
    </StatusPanel>
  );
}
