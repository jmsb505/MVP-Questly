import { StatusPanel } from "../components/StatusPanel";

export function HistoryPage() {
  return (
    <StatusPanel title="Completed quest history" eyebrow="Placeholder">
      Completed quest summaries and outcomes will appear here after the quest lifecycle is
      implemented.
    </StatusPanel>
  );
}
